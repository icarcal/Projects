var div, min, mod, multiply, sum;

multiply = function(x, y) {
  return x * y;
};

sum = function(x, y) {
  return x + y;
};

min = function(x, y) {
  return x - y;
};

div = function(x, y) {
  return x / y;
};

mod = function(x, y) {
  return x % y;
};
